version: '3.1'

services:

  db:
    image: postgres
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: "hullabulla"
    ports:
    - "5432:5432"

  adminer:
    image: adminer
    restart: unless-stopped
    ports:
      - "4747:8080"
